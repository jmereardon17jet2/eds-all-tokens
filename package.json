{
  "name": "@adobe/aem-boilerplate",
  "private": true,
  "version": "1.3.0",
  "description": "Starter project for Adobe Helix",
  "scripts": {
    "aemUp": "aem up",
    "start": "npm-run-all --parallel lint:watch aemUp",
    "postcss:watch": "postcss **/**/**/*.css --config postcss.config.js --watch",
    "build:watch": "run-p lint:watch postcss:watch",
    "lint:js": "eslint .",
    "lint:css": "stylelint blocks/**/*.css styles/*.css",
    "lint": "npm run lint:js && npm run lint:css",
    "lint:watch": "esw . -w --color --quiet",
    "update-tokens": "ncu @jet2/designsystem.tokens -u && npm install -D && node scripts/utils/generate-tokens.js",
    "update-tokens:holidays": "ncu @jet2/designsystem.tokens -u && npm install -D && node scripts/utils/generate-tokens.js --theme holidays",
    "update-tokens:city": "ncu @jet2/designsystem.tokens -u && npm install -D && node scripts/utils/generate-tokens.js --theme city",
    "update-tokens:global": "ncu @jet2/designsystem.tokens -u && npm install -D && node scripts/utils/generate-tokens.js --theme global",
    "update-tokens:indulgent": "ncu @jet2/designsystem.tokens -u && npm install -D && node scripts/utils/generate-tokens.js --theme indulgent",
    "update-tokens:vibe": "ncu @jet2/designsystem.tokens -u && npm install -D && node scripts/utils/generate-tokens.js --theme vibe",
    "update-tokens:villas": "ncu @jet2/designsystem.tokens -u && npm install -D && node scripts/utils/generate-tokens.js --theme villas",
    "new-block": "node scripts/utils/create-block.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/adobe/aem-boilerplate.git"
  },
  "author": "Adobe",
  "license": "Apache License 2.0",
  "bugs": {
    "url": "https://github.com/adobe/aem-boilerplate/issues"
  },
  "homepage": "https://github.com/adobe/aem-boilerplate#readme",
  "devDependencies": {
    "@adobe/aem-cli": "^16.8.6",
    "@babel/eslint-parser": "7.25.9",
    "@csstools/postcss-sass": "^5.1.1",
    "@jet2/designsystem.tokens": "^1.2.0",
    "clean-css": "^5.3.3",
    "eslint": "8.57.1",
    "eslint-config-airbnb-base": "15.0.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "2.31.0",
    "eslint-plugin-prettier": "^5.2.1",
    "eslint-watch": "^8.0.0",
    "glob": "^11.0.0",
    "npm-check-updates": "^17.1.13",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.4.49",
    "postcss-cli": "^11.0.0",
    "postcss-import": "^16.1.0",
    "postcss-preset-env": "^10.1.3",
    "postcss-reporter": "^7.1.0",
    "postcss-scss": "^4.0.9",
    "stylelint": "16.12.0",
    "stylelint-config-standard": "^36.0.1",
    "stylelint-value-no-unknown-custom-properties": "^6.0.1"
  }
}
